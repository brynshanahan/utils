{
  "name": "utils",
  "version": "1.0.0",
  "dependencies": {
    "@juggle/resize-observer": "^3.1.3",
    "intersection-observer": "^0.8.0"
  },
  "types": ".",
  "scripts": {
    "dev": "concurrently 'yarn compile-code' 'yarn compile-types'",
    "prod": "parcel build _src/*/*.ts --out-dir . && tsc",
    "compile-types": "node ./scripts/run-tsc.js",
    "compile-code": "parcel watch _src/*/*.ts --out-dir ."
  },
  "peerDependencies": {
    "@juggle/resize-observer": "^3.1.3",
    "intersection-observer": "^0.8.0"
  },
  "devDependencies": {
    "concurrently": "^5.1.0",
    "parcel": "^1.12.4",
    "typescript": "^3.8.3"
  }
}
